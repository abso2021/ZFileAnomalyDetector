# ZFileAnomalyDetector
This tool monitors a folder hourly, converts `.z` files to `.csv`, checks for data anomalies, and emails a summary of any issues to specified recipients. It automates data validation and alerting with minimal user input.
